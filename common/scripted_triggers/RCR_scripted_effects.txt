Rethenia_is_unified = {
	AND = {
		NOT = { country_exists = FEC }
		NOT = { country_exists = NOC }
		NOT = { country_exists = SOC }
		NOT = { country_exists = SIC }
		NOT = { country_exists = WEC }
		NOT = { country_exists = NWC }
		RCR = {
			owns_state = 6
			
		}
		RCR = {
			has_war = no
		}
	}
}

