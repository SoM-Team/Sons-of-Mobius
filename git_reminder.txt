DO NOT USE VSC BUTTONS

USE TERMINAL

Just do this: 

git add .
git commit -m message #WARNING! Replace 'message' with the actual message. In quotes if more than 1 word
git pull origin master --rebase # it's not advised to do so with not yet 
git push #use git push -f if you need to force push. Only when approved by at least 1 other developer